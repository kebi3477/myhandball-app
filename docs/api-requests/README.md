# API 작업 요청

이 폴더의 문서는 **`../myhandball-api` 저장소에서 작업할 Claude 세션에 그대로 붙여넣는 프롬프트**다.

앱 저장소에서는 API 코드를 직접 수정하지 않는다. 변경이 필요하면 여기에 자기완결적인 요청을
`<NNN>-<슬러그>.md` 형식으로 쓰고, 해당 기능은 API 배포 전까지 앱에서 목업/스텁으로 둔다.

형식은 루트 `CLAUDE.md`의 "작업 경계" 절 참조.
