# spec — 앱 구현용 추출본

`MyHandball v2.dc.html`에서 뽑았어요. 모든 파일이 256KiB 미만이에요.

- `logic.js` — 로직 클래스 원문 전체(~124KiB). 마크업의 `{{ }}` 값은 모두 여기 `renderVals()`에서 나와요. 아래 문서에 없는 값은 이 파일에서 변수명으로 검색하세요.
- `lessons.md` — 입문 가이드 LESSONS 5개(스텝, 퀴즈)
- `copy.md` — 스크립트가 만드는 문구와 분기
- `rules.md` — 랭킹, 팬덤, 승률, 비시즌, 경기 상태 계산 규칙
- `badges.md` — MY 탭 배지 3종
- `sheets.md` / `sheets.html` — 프로필 시트, 직관 기록 추가 시트(문구 + 마크업 원문)
- `moderation.md` / `moderation.html` — 응원글 신고·차단·차단 목록·빈 상태(신규)
